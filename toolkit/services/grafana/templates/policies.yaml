apiVersion: 1

# Route every alert to the homelab contact point (email + ntfy).
policies:
  - orgId: 1
    receiver: homelab
    group_by: ['grafana_folder', 'alertname']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
